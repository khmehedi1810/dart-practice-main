void main() {
  int axx = 20;
  double dxx = axx.toDouble();
  // without type cart it is not possible to assign

  num x = 10;
  // int a = x; // not possible, need type cast
  // down casting type cast

  int a = x as int;
  print(a);
}